# Sales / analytics UI — 2026-09-20

Implemented only in the assigned sales UI scope of `C:/Users/baran/Desktop/site/lunapot-panel`. No git operations, schema changes, deployment, live database access, shared-shell/auth/accounting/stock files, or preview-server changes.

## Files

- `public/performance-ui.js`
- `public/panorama-ui.js`
- `public/insights-design.css`
- `tests/sales-ui-contract.test.js` (new)
- `tests/sales-ui-browser.test.js` (new)
- `docs/SALES-UI-2026-09-20.md` (this report)

`public/date-range.js` and preexisting unowned files were not edited.

## Delivered behavior

- `#performance?view=sales` is the primary sales view. Date/channel scope, kind, search, sort and offering drilldown survive in hash parameters (`kind`, `q`, `order`, `item`). Explicit package mode and older result/sort package links remain supported; pending mode remains package-based.
- Names, composition, kinds and money come exclusively from historical `sales_items` / `period.sales`. No legacy stock-component profitability fallback. Full names remain readable on mobile. A stable server key groups identical offerings.
- Grouped gross revenue, gross product cost and cash; details show one-offering composition, contributing packages, original return components, signed sales/return cash effects and the server allocation explanation. Complete returns retain zero-unit rows; unbalanced partial returns show unknown net set count.
- Kind/search affect only the displayed/exported offering list. Authoritative date/channel totals and mutually exclusive failed-delivery/real-return impacts stay visible. Reconciliation checks revenue, cost and cash, including each source package so cancelling mismatches cannot falsely pass. Technical twin corrections are excluded from real-return counts.
- Unknown/hidden amounts stay unknown, including empty reports; known subtotals are explicitly separate from exact totals. Original package pagination, result filters and CSV columns remain. Sales CSV adds scope totals and return effects; package export prevents net-only values entering VAT-inclusive columns.
- Overview financial summary precedes the compact expandable date filter. “Satış biçimine göre kazanç” uses sales-only rankings, kind tags/filter, keyboard-accessible cash/revenue tabs and scoped drilldown links. Pending panels separate “Hazırlanan” and “Kargoda” using the finalized subgroups.
- Forest/paper CSS is scoped to these screens. No new shell or unrelated page.

## Verification

Final backend contract: `docs/SALES-MODEL-2026-09-20.md`. Final runs used main's restarted synthetic preview at `http://127.0.0.1:8791`; no server spawned here.

```powershell
node --test tests/sales-ui-contract.test.js tests/performance-tools.test.js tests/redesign-dates.test.js
$env:SALES_UI_BROWSER='1'
node --test tests/sales-ui-browser.test.js
```

- **42/42 targeted tests passed**: 15 new sales UI contract regressions, five existing performance tests, 22 date/pagination/overview regressions. Main updated the two obsolete stock-ranking fixtures in the latter file.
- **12/12 browser scenarios passed** (13 Node test records including parent): 360/390/1440 px × populated/missing/empty, reader at 390/1440, anonymous. Checks cover filters preserving totals/returns, three-component kit drilldown, search empty state, package CSV download, preparing/shipped, overview summary order, ranking keyboard navigation, hidden amounts and no document overflow.
- Screenshots were captured in memory; mobile summary and desktop expanded kit were visually inspected. No screenshot artifacts or package installs. No page errors or external requests in the matrix.
- Final server allocation note was verified after restart; the old stock-component note no longer appears on offerings.

The browser test is opt-in and expects an already running local preview plus the existing Playwright/Chrome installation. Main owns broad final testing, service-worker publication/integration and deployment.

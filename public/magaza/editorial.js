/* Model selection stays with the reader across independent stories. */
(()=>{'use strict';const requested=new URLSearchParams(location.search).get('model');const model=['nova','luna'].includes(requested)?requested:'nova';document.querySelectorAll('[data-tech-model]').forEach(a=>{if(a.dataset.techModel===model)a.setAttribute('aria-current','page');else a.removeAttribute('aria-current')});document.querySelectorAll('.tech-other').forEach(a=>{const u=new URL(a.getAttribute('href'),location.href);u.searchParams.set('model',model);a.href=u.pathname.split('/').pop()+u.search});})();

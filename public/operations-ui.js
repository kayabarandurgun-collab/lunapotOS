import {summary} from './accounting-math.js';
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const money=n=>n==null?'Bilgi bekleniyor':new Intl.NumberFormat('tr-TR',{style:'currency',currency:'TRY'}).format(n/100);
const date=()=>new Date().toLocaleDateString('sv-SE',{timeZone:'Europe/Istanbul'});
const names={trendyol:'Trendyol',hepsiburada:'Hepsiburada',edm:'EDM'};
const kinds={orders:'Siparişler',sale:'Satış finans kayıtları',return:'İade finans kayıtları',deductions:'Kesinti faturaları',payments:'Hakediş kayıtları',finance:'Muhasebe hareketleri',commissions:'Güncel komisyon bilgisi'};
const field=(label,name,value='',type='text',extra='')=>'<label>'+esc(label)+'<input name="'+name+'" type="'+type+'" value="'+esc(value)+'" '+extra+'></label>';
const select=(label,name,values)=>'<label>'+esc(label)+'<select name="'+name+'">'+values.map(([v,t])=>'<option value="'+esc(v)+'">'+esc(t)+'</option>').join('')+'</select></label>';
const link=(href,title,desc)=>'<a class="step" href="'+href+'"><span class="step-number">→</span><div><strong>'+title+'</strong><p>'+desc+'</p></div></a>';
const heading=(title,sub)=>'<div class="page-heading"><div><h1>'+title+'</h1><p>'+sub+'</p></div></div>';
const table=(heads,rows)=>rows.length?'<div class="table-wrap"><table><thead><tr>'+heads.map(x=>'<th>'+x+'</th>').join('')+'</tr></thead><tbody>'+rows.map(row=>'<tr>'+row.map(x=>'<td>'+x+'</td>').join('')+'</tr>').join('')+'</tbody></table></div>':'<div class="empty"><h3>Henüz kayıt yok</h3><p>Bağlantıdan alınan kayıtlar burada görünecek.</p></div>';
function download(data,name){const url=URL.createObjectURL(new Blob([JSON.stringify(data,null,2)],{type:'application/json'})),a=document.createElement('a');a.href=url;a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);}
export function mountOperations(root,namespace,view){
 const abort=new AbortController(),$=s=>root.querySelector(s);let state=null,busy=false,recordPage=0,recordProvider='',recordKind='';
 const api=async(path,body)=>{const r=await fetch('/api/'+namespace+path,{signal:abort.signal,headers:{'Content-Type':'application/json'},...(body===undefined?{}:{method:'POST',body:JSON.stringify(body)})});let x;try{x=await r.json();}catch{throw Error('Sunucuya ulaşılamadı.');}if(!r.ok)throw Error(x.error||'İşlem tamamlanamadı.');return x;};
 const notice=m=>{const box=$('#op-error');if(box){box.hidden=false;box.textContent=m;}};
 const close=()=>{$('dialog')?.close();$('dialog')?.remove();};
 function dialog(title,action,body){close();root.insertAdjacentHTML('beforeend','<dialog><form data-op-form="'+action+'"><div class="dialog-heading"><h2>'+title+'</h2><button class="icon-button" type="button" data-op="close" aria-label="Kapat">×</button></div><div class="form-body">'+body+'<p class="error" id="op-form-error" role="alert"></p></div><div class="dialog-footer"><button class="secondary" type="button" data-op="close">Vazgeç</button><button class="primary" type="submit">Devam et</button></div></form></dialog>');$('dialog').showModal();}
 async function load(){
  root.innerHTML='<p class="loading">Çalışma alanı hazırlanıyor…</p>';
  if(view==='overview'){
   const [ac,ledger,orders,connections,settings]=await Promise.all([api(''),api('/ledger'),api('/orders'),api('/connections'),api('/settings')]);
   if(abort.signal.aborted)return;
   const totals=summary(ac.sales,ac.expenses),configured=connections.providers.filter(p=>p.configured).length,owed=ledger.parties.reduce((s,p)=>s+Math.max(0,-p.balance_cents),0),available=ac.stock.filter(p=>p.quantity_milli>0).length;
   root.innerHTML=heading('Bugün işin nasıl gidiyor?','E-ticaret çalışma alanı · Son 30 günün satışları ve güncel stok durumu')+'<div id="op-error" class="notice" hidden></div><div class="stats">'+[
    ['Net satış',money(totals.revenue),'İadeler düşülmüş · KDV hariç'],
    ['Faaliyet sonucu',totals.profit===null||ac.pending_fee_cents>0?'Hesap eksik':money(totals.profit),ac.pending_fee_cents>0?money(ac.pending_fee_cents)+' kesinti dağıtılmayı bekliyor':totals.missing?totals.missing+' satışta kesinti bilgisi eksik':'Kayıtlı maliyetler ve genel giderler'],
    ['Açık sipariş',String(orders.counts.filter(p=>['draft','reserved'].includes(p.status)).reduce((s,p)=>s+p.count,0)),'İnceleme ve hazırlık bekleyen paketler'],
    ['Cari borç',money(owed),'Tüm tarihler · KDV dahil bakiye']
   ].map(([label,value,help],i)=>'<article class="stat '+(i===1?'highlight':'')+'"><span>'+label+'</span><strong>'+value+'</strong><small>'+help+'</small></article>').join('')+'</div><div class="dashboard-grid"><section class="card"><div class="card-heading"><h2>İşe başlamak için</h2><span class="pill">'+[!!settings.settings.tax_id,ac.stock.length>0,available>0,configured>0].filter(Boolean).length+'/4 hazır</span></div>'+
   link('#settings','Şirketini tanımla',settings.settings.tax_id?'Alış faturalarının alıcısı doğrulanıyor.':'Unvan ve vergi numarası faturaların doğru alana gelmesini sağlar.')+
   link('#stock','Ürünlerini ve açılış stoğunu ekle',ac.stock.length+' ürün · '+available+' üründe stok var.')+
   link('#pricing','Maliyet ve tarifelerini belirle','Paket ölçüsü, kargo ve komisyonla satıştan önce kârını gör.')+
   link('#integrations','Satış kanallarını bağla',configured+' bağlantıda erişim bilgisi tanımlı. Canlı durum Bağlantılar ekranında.')+'</section><section class="card"><div class="card-heading"><h2>Günün çalışma akışı</h2></div>'+
   link('#orders','Siparişleri hazırla','Ürün eşleştir → stok ayır → kargoya ver.')+link('#invoices','Alış ve mal teslimlerini işle','Fatura borcu ve depoya gelen miktar ayrı takip edilir.')+
   link('#ledger','Cari ve nakit durumuna bak','Müşteri, tedarikçi, pazaryeri, banka ve kasa.')+link('#sales','Kesintileri ve kârı doğrula','Tahminler ile gelen belgeleri karşılaştır.')+
   '</section></div><div class="notice subtle">Bu alanın carileri, stokları ve raporları Lunapot üretim panelinden ayrıdır. Rakamlar yalnızca kaydedilmiş işlemleri içerir; henüz bağlanmamış mağaza satışları dahil değildir.</div>';
  }else if(view==='settings'){
   state=await api('/settings');if(abort.signal.aborted)return;
   root.innerHTML=heading('Şirket ve yedek','Bu bilgiler yalnızca '+(namespace==='ec'?'E-Ticaret':'Lunapot')+' çalışma alanına aittir.')+'<div id="op-error" class="notice" hidden></div><div class="v2-grid cols-2"><section class="card"><div class="card-heading"><h2>Faturaların doğru adresi</h2></div><form data-op-form="settings" class="form-body">'+field('Ticari unvan','legal_name',state.settings.legal_name,'text','maxlength="200" required')+field('VKN / TCKN','tax_id',state.settings.tax_id,'text','pattern="[0-9]{10,11}" required inputmode="numeric"')+'<p class="help">İki işletme aynı şirkete aitse aynı vergi numarası kullanılabilir. Defterler yine ayrı kalır; aynı fatura ikinci kez işlenmez.</p><button class="primary" type="submit">Şirket bilgilerini kaydet</button></form></section><section class="card"><div class="card-heading"><h2>Verilerin sende kalsın</h2></div><div class="form-body"><p>Bu çalışma alanının ürün, cari, fatura ve hareketlerini JSON olarak indir.</p><button class="secondary" data-op="backup">İş verilerini indir</button><p class="help">Şifreler ve bağlantı anahtarları dahil edilmez. Bu dosya iş verileri arşividir; otomatik geri yükleme özelliği henüz yoktur.</p><div class="notice subtle">Lunapot AI kapalı. Ücretli yapay zekâ servisi bağlı değil.</div></div></section></div>';
  }else{
   state=await api('/connections');if(abort.signal.aborted)return;
   root.innerHTML=heading('Mağazalarınla bağlantı kur','Siparişleri ve finans belgelerini resmî servislerden al, kaynaklarını takip et.')+'<div id="op-error" class="notice" hidden></div>'+(!state.encryption_ready?'<div class="notice">Güvenli anahtar kasası henüz sunucuda kurulmadı. Erişim bilgileri kaydedilmez.</div>':'')+'<div class="recipe-grid">'+state.providers.map(p=>'<section class="card recipe-card"><span class="pill '+(p.configured?'':'neutral')+'">'+(p.configured?(p.stale?'Veri güncel değil':p.last_success_at?'Veri alındı':'Test bekliyor'):'Bağlantı bekliyor')+'</span><h2>'+esc(p.name)+'</h2><p>'+esc(p.id==='edm'?'EDM servis erişimi doğrulanmayı bekliyor. Alış Faturaları ekranında XML içe aktarımı kullanılabilir.':p.configured?'Son başarılı işlem: '+(p.last_success_at||'Henüz yok'):'Mağazana ait API erişim bilgilerini güvenli kasaya ekle.')+'</p>'+(p.last_error?'<p class="error">'+esc(p.last_error)+'</p>':'')+(p.id!=='edm'?'<div class="ac-actions"><button class="secondary" data-op="configure" data-provider="'+p.id+'" '+(!state.encryption_ready?'disabled':'')+'>'+ (p.configured?'Bağlantıyı güncelle':'Bağlantı kur')+'</button><button class="primary" data-op="sync" data-provider="'+p.id+'" '+(!p.configured?'disabled':'')+'>Verileri al</button></div>':'<a href="#invoices" class="text-button">Fatura içe aktar →</a>')+'</section>').join('')+'</div><div class="notice subtle">Siparişler önce incelemeye alınır. Finans kayıtlarının alınması, kesintilerin satışlara işlendiği veya paranın bankaya geçtiği anlamına gelmez. Komisyon tarifeleri Fiyat ve kâr ekranında tarihleriyle kontrol edilir.</div><section class="card breakdown"><div class="card-heading"><h2>Kaynak kayıtları</h2></div><form class="ac-filters" data-op-form="records">'+select('Kaynak','provider',[['trendyol','Trendyol'],['hepsiburada','Hepsiburada']])+select('Veri türü','kind',Object.entries(kinds))+'<button class="secondary" type="submit">Kayıtları göster</button></form><div id="op-records"></div></section><section class="card breakdown"><div class="card-heading"><h2>Son bağlantı işlemleri</h2></div>'+table(['Zaman','Kanal','Kayıt','Sonuç'],state.runs.map(r=>[esc(r.created_at),esc(names[r.provider]||r.provider),esc(r.record_count),esc(r.message)]))+'</section>';
  }
 }
 root.addEventListener('click',async e=>{const b=e.target.closest('[data-op]');if(!b||busy)return;const action=b.dataset.op,provider=b.dataset.provider;try{
  if(action==='close'){close();return;}
  if(action==='configure'){dialog(names[provider]+' bağlantısı','configure',field('Sağlayıcı','provider',provider,'hidden')+field(provider==='trendyol'?'Satıcı ID':'Merchant ID','seller_id','','text','required autocomplete="off"')+field(provider==='trendyol'?'API key':'API kullanıcı adı','key','','password','required autocomplete="off"')+field(provider==='trendyol'?'API secret':'API şifresi / servis anahtarı','secret','','password','required autocomplete="off"')+field('Entegrasyon kimliği (User-Agent)','user_agent','','text','required autocomplete="off"')+'<p class="help">Mağaza panelindeki API bilgilerini kullanın. Bu alan mağaza giriş şifresi için değildir. Kayıttan sonra anahtarlar tekrar gösterilmez.</p>');return;}
  if(action==='sync'){const p=state.providers.find(p=>p.id===provider);dialog(names[provider]+' · Verileri al','sync',field('Sağlayıcı','provider',provider,'hidden')+select('Alınacak kayıtlar','kind',p.capabilities.map(k=>[k,kinds[k]||k]))+'<div class="field-grid">'+field('Başlangıç','from',new Date(Date.now()-6*86400000).toISOString().slice(0,10),'date','required')+field('Bitiş','to',date(),'date','required')+'</div>'+field('Sayfa (ilk sayfa 0)','page',0,'number','required min="0" max="10000" step="1"')+field('HB komisyon SKU kodları (virgülle ayır)','skus','','text','maxlength="4000"')+'<p class="help">Her işlem bir sayfa alır. Sonraki sayfa varsa sonuçta belirtilir. Mağazada fiyat, stok veya sipariş durumu değiştirilmez.</p>');return;}
  if(action==='backup'){busy=true;b.disabled=true;download(await api('/settings/backup'),'lunapot-'+namespace+'-'+date()+'.json');notice('İş verileri dosyası indirildi.');}
  if(action==='next-records'){recordPage++;await records();}
 }catch(error){notice(error.message);}finally{busy=false;b.disabled=false;}},{signal:abort.signal});
 async function records(){const r=await api('/connections/records?provider='+encodeURIComponent(recordProvider)+'&kind='+encodeURIComponent(recordKind)+'&page='+recordPage);$('#op-records').innerHTML='<p class="pad muted">Sayfa '+(recordPage+1)+' · Kaynak kayıtları henüz mali sonuç değildir.</p>'+table(['Kaynak kimliği','Son görülme','İçerik'],r.records.map(r=>[esc(r.external_id),esc(r.last_seen_at),'<details><summary>Kaydı incele</summary><pre>'+esc(JSON.stringify(r.payload,null,2))+'</pre></details>']))+(r.hasMore?'<button class="secondary" data-op="next-records">Sonraki sayfa</button>':'');}
 root.addEventListener('submit',async e=>{if(!e.target.dataset.opForm)return;e.preventDefault();if(busy)return;busy=true;const b=e.submitter;if(b)b.disabled=true;try{const x=Object.fromEntries(new FormData(e.target)),action=e.target.dataset.opForm;
  if(action==='settings'){await api('/settings',x);notice('Şirket bilgileri kaydedildi.');}
  if(action==='configure'){await api('/connections/'+x.provider+'/configure',x);close();await load();notice('Erişim bilgileri kaydedildi. Bağlantıyı doğrulamak için verileri alın.');}
  if(action==='sync'){const r=await api('/connections/'+x.provider+'/sync',{...x,page:Number(x.page),skus:x.skus.split(',').map(s=>s.trim()).filter(Boolean)});close();await load();notice((r.message||'Kaynak kayıtları alındı.')+(r.orderImportWarning?' '+r.orderImportWarning:'')+(r.hasMore?' Sonraki sayfa: '+(Number(x.page)+1):''));}
  if(action==='records'){recordPage=0;recordProvider=x.provider;recordKind=x.kind;await records();}
 }catch(error){const box=$('#op-form-error');if(box)box.textContent=error.message;else notice(error.message);}finally{busy=false;if(b)b.disabled=false;}},{signal:abort.signal});
 load().catch(e=>{if(e.name!=='AbortError')root.innerHTML='<div class="notice">'+esc(e.message)+'</div>';});
 return ()=>{abort.abort();close();};
}

import test from 'node:test';import assert from 'node:assert/strict';import {permit,filterInsights} from '../src/permission-policy.js';import {parsePermissions,level} from '../public/permissions.js';
const user={owner:false,ec_access:'write',lp_access:'write',permissions:parsePermissions({ec:{orders:'write',pricing:'read'},lp:{products:'write',recipes:'read'}})};
test('İşlem yetkisi ve ayrı kalıcı silme onayı; bilinmeyen rotalar kapalıdır',()=>{permit(user,'/api/ec/orders/a/ship','POST');permit(user,'/api/ec/pricing/quote','POST');assert.throws(()=>permit(user,'/api/ec/pricing/profiles','POST'));assert.throws(()=>permit(user,'/api/ec/sales/a/fees','POST'));assert.throws(()=>permit(user,'/api/products/a','DELETE'));assert.throws(()=>permit({...user,permissions:{...user.permissions,delete_records:true}},'/api/products/a','DELETE'));permit({...user,permissions:parsePermissions({lp:{products:'write',recipes:'write'},delete_records:true})},'/api/products/a','DELETE');assert.throws(()=>permit(user,'/api/new-backdoor','POST'));assert.equal(level({...user,ec_access:'read'},'ec','orders'),'read');});
test('Sipariş ayrıntısı kapalı alış faturası belgelerini yan yoldan göstermez',()=>{const x=filterInsights({purchase_invoices:[{invoice_no:'SECRET'}],purchase_invoices_truncated:true,fee_evidence:[{invoice_id:'SECRET',invoice_no:'SECRET',invoice_date:'2026-09-09',description:'SECRET',amount_cents:100,component:'shipping'}]},user,'ec');assert.deepEqual(x.purchase_invoices,[]);assert.equal(JSON.stringify(x).includes('SECRET'),false);assert.equal(x.fee_evidence[0].amount_cents,100);});

test('Yeni personel formu varsayılan olarak bütün ekranları kapalı başlatır',()=>{assert.equal(level(undefined,'ec','stock'),'none');assert.equal(level(undefined,'lp','recipes'),'none');});
